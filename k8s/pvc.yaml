apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: playlister-data
  namespace: playlister
spec:
  accessModes:
    - ReadWriteOnce  # Changed from ReadWriteMany for single-node compatibility
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path  # Use your default storage class
